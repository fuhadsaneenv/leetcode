/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var searchRange = function(nums, target) {
    // let res=[-1,-1]
    // res[0]=lastIndexOf()
    // res[1]=

    return [nums.indexOf(target),nums.lastIndexOf(target)]
    
};